# System-1 Port Notes

System-1 is the first fully modular subsystem migration on the vNext runtime branch. It provides deterministic cognitive mapping, bounded graph metrics, source-aware memory recall, and candidate-only memory writes.

Local validation completed:

```text
5 passed
uploaded legacy schema parsed as valid JSON
```

A GitHub Actions workflow is not yet committed. Until CI is added and a run succeeds, the evidence is local test execution only.
