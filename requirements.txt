# Fred-OS vNext uses pyproject.toml as the authoritative dependency manifest.
# Install development tooling with:
#   python -m pip install -e '.[dev]'
#
# This file intentionally avoids a machine-specific environment export.
pytest>=8.0
